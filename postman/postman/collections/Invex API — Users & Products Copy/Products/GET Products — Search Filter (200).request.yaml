$kind: http-request
url: "{{baseUrl}}/products?search=test"
method: GET
headers:
  Authorization: Bearer {{adminToken}}
queryParams:
  search: test
scripts:
  - type: afterResponse
    code: pm.test('Status is 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 2000
